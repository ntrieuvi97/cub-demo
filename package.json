{
  "name": "cub-demo",
  "version": "1.0.0",
  "description": "Cucumber + Playwright (TypeScript) automation tests",
  "main": "src/index.ts",
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require src/steps/**/*.ts tests/**/*.feature"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@playwright/test": "^1.44.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0"
  }
}
